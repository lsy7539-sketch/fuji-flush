// Shared between 혼자하기 (localMode.ts) and 같이하기's server-side AI seats
// (rooms.ts) so the two don't drift into two different name pools.
export const BOT_NAME_POOL = ["카리나", "안유진", "장원영", "수지", "윈터", "미나미", "원이"];
