# Fuji Flush 웹게임 구현 최종 프롬프트

한국어판 **후지 플러시(Fuji Flush) 공식 게임 설명서**의 규칙을 기준으로 웹 카드게임을 구현해줘.

임의로 규칙을 추가하거나 일반적인 카드게임 규칙을 추측해서 구현하지 말고, 아래 명시된 규칙을 게임 엔진의 기준으로 사용한다.

특히 다음 개념을 정확하게 구현해야 한다.

* 카드 내기
* 낮은 카드 밀어내기(Flush)
* 같은 숫자 카드 연합
* 연합 숫자의 합
* 자기 차례가 다시 왔을 때 카드 버리기
* 연합한 플레이어들의 동시 카드 버리기
* 승리 판정
* 싱글 플레이 AI 순차 진행
* 사용자가 이해할 수 있을 정도의 느린 게임 템포

---

# 1. 게임 기본 정보

플레이 가능 인원:

```text
3~8명
```

게임의 목표는:

**자기 앞에 놓인 카드와 손에 있는 카드를 가장 먼저 모두 없애는 것**

이다.

플레이어는 높은 숫자의 카드를 이용해 상대의 낮은 카드를 밀어낼 수 있다.

또한 다른 플레이어가 낸 카드와 **같은 숫자의 카드**를 내면 서로 연합하여 힘을 합칠 수 있다.

---

# 2. 카드 구성

총 카드 수는 정확히 **90장**이다.

공식 설명서 기준 카드 구성:

```text
2  : 16장
3  : 12장
4  : 9장
5  : 8장

6  : 6장
7  : 6장

8  : 5장

9  : 4장
10 : 4장
11 : 4장

12 : 3장
13 : 3장
14 : 3장

15 : 2장

16 : 1장
17 : 1장
18 : 1장
19 : 1장
20 : 1장
```

즉:

**16, 17, 18, 19, 20 카드는 모두 존재한다.**

각각 1장씩이다.

총합은 반드시 90장이 되어야 한다.

게임 초기화 시 개발 모드에서 다음 검증도 수행한다.

```text
deck.length === 90
```

---

# 3. 싱글 플레이 인원 선택

싱글 플레이에서는:

```text
사용자 1명 + AI 여러 명
```

으로 플레이한다.

게임 시작 화면에서 사용자가 AI 플레이어 수를 선택할 수 있어야 한다.

선택 범위:

```text
AI 2명 → 총 3명
AI 3명 → 총 4명
AI 4명 → 총 5명
AI 5명 → 총 6명
AI 6명 → 총 7명
AI 7명 → 총 8명
```

기본 선택:

```text
AI 3명
총 4인 플레이
```

UI 예:

```text
싱글 플레이

AI 플레이어 수

[-] 3명 [+]

총 플레이어: 4명

[게임 시작]
```

AI 수는 하드코딩하지 않는다.

반드시:

```text
players.length
```

를 기준으로 모든 턴 로직이 동적으로 동작해야 한다.

---

# 4. 카드 배분

90장의 카드를 모두 섞는다.

### 3~6명

각 플레이어에게:

```text
6장
```

씩 나누어 준다.

### 7~8명

각 플레이어에게:

```text
5장
```

씩 나누어 준다.

나머지 카드는 중앙에 뒤집어서 **Draw Pile / 카드 더미**로 둔다.

---

# 5. 플레이어 순서

시작 플레이어를 정한 후 시계 방향으로 진행한다.

싱글 플레이에서는 기본적으로:

```text
사용자
→ AI 1
→ AI 2
→ AI 3
→ ...
→ 마지막 AI
→ 사용자
```

순서다.

다음 플레이어:

```javascript
currentPlayerIndex =
    (currentPlayerIndex + 1) % players.length;
```

형태로 구현한다.

---

# 6. 플레이어 상태

플레이어 데이터는 최소 다음 구조를 가진다.

```javascript
Player {
    id,
    name,
    hand: [],
    activeCard: null,
    isHuman,
    isWinner
}
```

`hand`

= 아직 손에 들고 있는 카드.

`activeCard`

= 이전 자기 차례에 자기 앞에 내려놓은 카드.

중요:

**activeCard와 hand를 서로 다른 상태로 관리한다.**

---

# 7. 기본적인 카드 내기

자기 차례가 되면 손에 있는 카드 중 정확히 **1장**을 자기 앞에 앞면으로 내려놓는다.

어떤 카드든 낼 수 있다.

현재 테이블에 더 높은 숫자가 있다고 해서 낮은 숫자를 못 내는 것은 아니다.

---

# 8. 가장 기본적인 Flush 규칙

새로운 카드가 나왔을 때:

**새로 나온 카드보다 숫자가 낮은 기존 카드들은 밀려난다.**

예:

```text
연수 = 6
```

다음 지수가:

```text
8
```

을 냈다.

그러면:

```text
8 > 6
```

이므로 연수의 6이 밀려난다.

연수는:

```text
자기 앞의 6을 버리고
카드 더미에서 새로운 카드 1장을 뽑는다.
```

즉 Flush는:

```text
activeCard 제거
+
Draw 1
```

이다.

---

# 9. Flush당해도 카드 수는 줄지 않는다

예:

연수가 원래:

```text
손 5장
+
앞에 6 한 장
```

을 가지고 있었다고 하자.

6이 Flush되면:

```text
6을 버림
+
새 카드 1장 Draw
```

이므로 전체적으로 가지고 있는 카드 숫자는 줄지 않는다.

따라서 승리에 가까워지는 행동은 Flush가 아니다.

---

# 10. 낮은 카드를 새로 내도 죽지 않는다

매우 중요하다.

현재:

```text
지수 = 8
```

인 상황에서 나미가:

```text
7
```

을 냈다.

7은 8보다 낮지만:

**나미의 7은 버려지지 않는다.**

결과:

```text
지수 = 8
나미 = 7
```

두 카드 모두 테이블에 남는다.

즉:

**기존의 높은 카드가 나중에 등장한 낮은 카드를 공격하는 방식이 아니다.**

새로운 카드가 등장했을 때 그 카드가 **기존의 낮은 카드들을 밀어내는 방식**이다.

---

# 11. 같은 숫자의 카드 = 연합

다른 플레이어 앞에 있는 카드와 **같은 숫자의 카드**를 냈다면 두 플레이어는 연합한다.

예:

```text
나미 = 7
```

가희가:

```text
7
```

을 냈다.

그러면:

```text
나미 7 + 가희 7
```

이 연합한다.

연합의 힘은:

```text
7 + 7 = 14
```

이다.

---

# 12. 연합의 힘 계산

같은 숫자를 낸 플레이어가 N명이라면:

```text
연합 POWER = 카드 숫자 × 연합 인원
```

예:

```text
5 + 5 = 10

7 + 7 = 14

4 + 4 + 4 = 12

6 + 6 + 6 = 18
```

이다.

---

# 13. 연합은 반드시 '원래 카드 숫자'가 같아야 한다

이 규칙은 절대 틀리면 안 된다.

예:

```text
A = 5
B = 5
```

두 사람의 연합 POWER는:

```text
10
```

이다.

그런데 C가 실제 카드:

```text
10
```

을 냈다고 하자.

C는 5 연합에 참여할 수 없다.

왜냐하면:

```text
A의 실제 숫자 = 5
B의 실제 숫자 = 5
C의 실제 숫자 = 10
```

이기 때문이다.

따라서:

```text
A+B의 5 연합 → POWER 10

C의 10 → POWER 10
```

이라는 **서로 다른 두 그룹**이다.

---

# 14. 합산 결과가 같아도 연합하지 않는다

다음은 잘못된 처리다.

```text
5 + 5 = 10

그러므로 실제 10 카드와 연합한다.
```

절대로 이렇게 구현하지 않는다.

연합 조건은 오직:

```javascript
cardA.value === cardB.value
```

이다.

`combinedPower`가 같은 것은 연합 조건이 아니다.

---

# 15. 연합이 만들어지면 그 힘으로 상대를 밀어낸다

공식 설명서 예시:

현재:

```text
지수 = 8
나미 = 7
```

가희가:

```text
7
```

을 냈다.

나미와 가희의 카드가 연합한다.

```text
7 + 7 = 14
```

이제:

```text
연합 14 > 지수 8
```

이므로 지수의 8이 밀려난다.

지수:

```text
8 버림
+
새 카드 1장 Draw
```

현재 테이블:

```text
나미 = 7
가희 = 7

연합 POWER = 14
```

---

# 16. 연합보다 약한 숫자는 연합을 이길 수 없다

현재:

```text
나미 = 7
가희 = 7

연합 POWER = 14
```

연수가:

```text
9
```

를 냈다고 하자.

9는 개별 카드 7보다 높지만:

```text
9 < 14
```

이다.

따라서 7 연합을 밀어내지 못한다.

현재:

```text
7 연합 POWER 14
연수 = 9
```

상태가 유지된다.

---

# 17. 실제 숫자 14도 7 연합과 연합하지 않는다

현재:

```text
나미 = 7
가희 = 7

연합 POWER = 14
```

지수가 실제 숫자:

```text
14
```

카드를 냈다.

연합 POWER와 숫자가 같지만:

**지수의 14는 7 연합에 참여하지 않는다.**

7 연합은:

```text
실제 카드 숫자 7
```

끼리 만들어졌기 때문이다.

---

# 18. 같은 힘은 이기지 못한다

현재:

```text
나미 7 + 가희 7
= POWER 14
```

지수가 실제:

```text
14
```

를 냈다.

비교:

```text
14 == 14
```

이므로 지수의 14는 7 연합을 밀어내지 못한다.

Flush 조건은:

```text
newPower > existingPower
```

이다.

같으면 Flush하지 않는다.

---

# 19. 하지만 14는 기존 9를 밀어낼 수 있다

위 상황에 연수의 9가 있었다면:

```text
지수의 14 > 연수의 9
```

이므로:

```text
연수의 9 → Flush
연수 Draw 1
```

한다.

그러나:

```text
지수 14
vs
7+7 연합 14
```

는 동점이므로 7 연합은 그대로 유지된다.

---

# 20. 자기 차례가 다시 왔을 때

이 규칙이 승리를 위해 매우 중요하다.

어떤 플레이어가 카드를 냈는데,

다른 플레이어들의 차례가 진행되는 동안 더 높은 카드 또는 연합에게 밀려나지 않았다면,

다시 자신의 차례가 시작될 때 그 카드가 여전히 자기 앞에 남아 있다.

그러면:

**그 카드를 버린다.**

그리고 이번에는:

**새 카드를 뽑지 않는다.**

---

# 21. 성공적으로 버린 카드와 Flush의 차이

### Flush

다른 플레이어에게 밀려남.

```text
기존 카드 버림
+
새 카드 1장 Draw
```

전체 카드 수 변화:

```text
0
```

### 자기 차례까지 살아남음

```text
기존 카드 버림
+
Draw 없음
```

전체 카드 수 변화:

```text
-1
```

따라서 플레이어는 승리에 한 걸음 가까워진다.

---

# 22. 자기 차례 시작 처리 순서

플레이어의 턴이 시작되면:

### STEP 1

자기 앞에 `activeCard`가 있는지 확인한다.

### STEP 2

있다면 해당 카드는 한 바퀴를 살아남은 것이다.

따라서 카드를 버린다.

### STEP 3

새 카드를 Draw하지 않는다.

### STEP 4

연합 여부를 확인하고 아래의 **연합 동시 성공 처리**를 적용한다.

### STEP 5

승리 여부 확인.

### STEP 6

아직 게임이 끝나지 않았다면 손에서 새로운 카드 한 장을 낸다.

---

# 23. 연합한 카드가 살아남은 경우

이 부분은 반드시 정확하게 구현한다.

현재:

```text
나미 = 7
가희 = 7
```

두 사람은 연합 상태다.

게임이 진행된 뒤 **나미의 차례가 시작되었다.**

나미의 7이 아직 자기 앞에 남아 있다.

따라서 나미는 자신의 7을 버린다.

그런데 가희의 7도 나미와 연합하고 있었다.

따라서:

**가희도 자신의 7을 함께 버린다.**

---

# 24. 연합 성공 시 모두 새 카드를 뽑지 않는다

위 상황:

```text
나미 7
가희 7
```

나미 차례 시작.

연합이 살아남았다.

결과:

```text
나미 7 → 버림
가희 7 → 버림
```

그리고:

```text
나미 Draw 없음
가희 Draw 없음
```

이다.

두 플레이어 모두 카드 수가 각각 1장씩 감소한다.

---

# 25. 연합 성공 후 현재 플레이어만 새 카드를 낸다

연합 성공으로:

```text
나미와 가희
```

둘 다 카드를 버렸다고 하자.

현재 실제 차례는 나미다.

따라서:

```text
나미 → 새 카드 한 장 플레이
가희 → 아무것도 하지 않고 자기 차례를 기다림
```

이다.

가희가 즉시 새 카드를 내면 안 된다.

---

# 26. 다른 사람 덕분에 연합 카드가 먼저 제거된 플레이어

위 상황에서 가희는 자신의 차례가 오기 전에 연합 성공으로 이미 activeCard가 제거되었다.

나중에 가희 차례가 시작되면:

```text
가희.activeCard === null
```

이다.

따라서 가희는 기존 카드 버리기 단계를 생략하고:

**바로 손에서 새로운 카드 1장을 플레이한다.**

---

# 27. 연합이 Flush되는 경우

현재:

```text
A = 7
B = 7

연합 POWER = 14
```

C가:

```text
15
```

를 냈다.

```text
15 > 14
```

이므로 연합 전체가 밀려난다.

결과:

```text
A의 7 버림
B의 7 버림

A Draw 1
B Draw 1
```

연합원 모두 Flush된다.

---

# 28. 게임 엔진에서 연합을 계산하는 방법

연합은 영구적인 팀 객체로 저장하지 않는 것을 권장한다.

현재 살아있는 `activeCard`들을 실제 카드 숫자로 그룹화한다.

예:

```text
A = 7
B = 7
C = 14
D = 9
```

그룹 계산 결과:

```javascript
[
    {
        value: 7,
        members: [A, B],
        power: 14
    },

    {
        value: 14,
        members: [C],
        power: 14
    },

    {
        value: 9,
        members: [D],
        power: 9
    }
]
```

이 경우:

```text
7 연합 POWER 14
실제 14 카드 POWER 14
```

는 서로 별개다.

---

# 29. 새 카드 플레이 후 판정 방식

플레이어가 새로운 카드를 냈다면 다음 순서로 계산한다.

```text
1. 해당 카드를 activeCard로 등록
2. 같은 실제 숫자의 activeCard 탐색
3. 같은 숫자가 있다면 연합 형성
4. 새 카드가 포함된 그룹의 POWER 계산
5. 기존 다른 그룹들과 POWER 비교
6. 새 그룹보다 낮은 기존 그룹 Flush
7. 동점 또는 높은 그룹은 유지
8. 상태 갱신
9. 다음 플레이어 진행
```

---

# 30. 매우 중요한 방향성

게임은 과거 카드가 새 카드를 공격하는 구조가 아니다.

예:

```text
기존 = 8
새 카드 = 7
```

이면:

```text
8 유지
7 유지
```

다음:

```text
새 카드 = 6
```

이라면 그 자체로 기존 7이나 8이 아무것도 하지 않는다.

그러나 이후 누군가:

```text
9
```

를 내면 새로운 9의 힘으로 더 낮은 기존 카드/그룹들이 밀려난다.

---

# 31. 공식 설명서 다인 플레이 예시를 그대로 테스트 케이스로 구현

아래 시나리오는 반드시 자동 테스트 또는 개발자 테스트 모드에서 재현 가능해야 한다.

플레이어:

```text
연수
지수
나미
가희
```

---

## TURN 1

연수:

```text
6
```

플레이.

결과:

```text
연수 = 6
```

---

## TURN 2

지수:

```text
8
```

플레이.

```text
8 > 6
```

따라서:

```text
연수의 6 Flush
연수 Draw 1
```

현재:

```text
지수 = 8
```

---

## TURN 3

나미:

```text
7
```

플레이.

```text
7 < 8
```

이지만 아무 일도 발생하지 않는다.

현재:

```text
지수 = 8
나미 = 7
```

---

## TURN 4

가희:

```text
7
```

플레이.

나미의 7과 연합.

```text
7 + 7 = 14
```

14가 지수의 8보다 높다.

따라서:

```text
지수의 8 Flush
지수 Draw 1
```

현재:

```text
나미 = 7
가희 = 7

연합 POWER 14
```

---

## TURN 5

연수:

```text
9
```

플레이.

현재 7 연합은:

```text
POWER 14
```

이다.

```text
9 < 14
```

이므로 연수의 9는 두 7을 밀어낼 수 없다.

그러나 연수의 9 자체도 버려지지 않는다.

현재:

```text
연수 = 9

나미 = 7
가희 = 7

7 연합 POWER = 14
```

---

## TURN 6

지수:

```text
14
```

플레이.

중요:

지수의 실제 숫자 14는:

```text
7 + 7 = POWER 14
```

연합에 참여할 수 없다.

7과 14는 실제 카드 숫자가 다르기 때문이다.

비교:

```text
지수 14
vs
7 연합 POWER 14
```

동점이므로 7 연합은 살아남는다.

하지만:

```text
14 > 연수 9
```

이므로:

```text
연수의 9 Flush
연수 Draw 1
```

한다.

현재:

```text
지수 = 14

나미 = 7
가희 = 7

연합 POWER = 14
```

---

## TURN 7

나미의 차례가 시작된다.

나미가 이전에 냈던:

```text
7
```

이 아직 남아 있다.

따라서 나미의 7은 성공적으로 버려진다.

그리고 가희의 7은 나미의 7과 연합 중이었다.

따라서:

```text
나미의 7 → 버림
가희의 7 → 버림
```

둘 다:

```text
Draw 없음
```

이다.

이제 두 플레이어 모두 승리에 한 장씩 가까워진다.

그 후 현재 플레이어인 나미만 새로운 카드를 플레이한다.

공식 설명서 예시에서는:

```text
나미 → 6
```

을 플레이한다.

---

# 32. 승리 조건

다음 조건을 만족하면 즉시 승리한다.

```text
자기 앞의 카드를 정상적으로 버렸으며
손에도 카드가 한 장도 남아 있지 않음
```

즉:

```javascript
player.hand.length === 0 &&
player.activeCard === null
```

이면 승리한다.

---

# 33. 다른 플레이어 턴에 승리 가능

연합 때문에 매우 중요하다.

A와 B가 같은 숫자로 연합했다.

A 차례가 돌아와 연합 카드가 성공적으로 버려졌다.

그 과정에서 B의 연합 카드도 같이 버려졌다.

만약 그것이 B의 마지막 카드였다면:

**B는 자기 차례가 아니더라도 즉시 승리할 수 있다.**

---

# 34. 공동 승리 가능

연합 성공으로 여러 플레이어가 동시에 마지막 카드를 버리는 경우:

```text
A 승리
B 승리
```

처럼 여러 명이 동시에 승리할 수 있다.

첫 번째 플레이어만 승자로 처리하지 않는다.

`winner` 하나보다는:

```javascript
winners: []
```

구조를 사용하는 것을 권장한다.

---

# 35. Draw Pile

Flush당한 플레이어는 중앙 카드 더미에서 카드 1장을 새로 가져온다.

정상적으로 한 바퀴를 살아남아서 카드를 버린 경우에는 Draw하지 않는다.

반드시 다음을 구분한다.

```text
Flush → Draw 1
성공적으로 카드 버림 → Draw 0
연합 Flush → 연합원 각각 Draw 1
연합 성공 → 연합원 모두 Draw 0
```

---

# 36. AI가 볼 수 있는 정보

AI는 다른 플레이어의 손에 있는 카드 정보를 볼 수 없다.

AI가 사용할 수 있는 정보:

```text
자기 손패
현재 테이블에 공개된 카드
현재 연합 상태
각 플레이어 남은 카드 수
Draw Pile 남은 장수
```

사용자의 손패나 다른 AI의 비공개 카드 값을 읽어서 판단하면 안 된다.

---

# 37. AI 기본 전략

AI가 완전히 랜덤으로 카드를 내지 않도록 한다.

우선순위 예:

### 1

같은 숫자의 공개 카드가 있고 자신의 카드로 연합할 수 있는지 확인.

### 2

연합했을 때 POWER가 증가하여 다른 플레이어를 Flush할 수 있다면 높은 우선순위.

### 3

자신이 가진 카드 하나로 낮은 상대 카드들을 Flush할 수 있는지 확인.

### 4

당장 공격할 수 없다면 비교적 낮은 카드를 소모하는 것도 고려.

### 5

16~20과 같은 희귀한 높은 숫자는 항상 첫 기회에 무조건 버리지 말고 상황에 따라 보존.

단 AI 난이도가 너무 높을 필요는 없다.

사람과 플레이하는 것처럼 자연스럽게 느껴지는 것이 우선이다.

---

# 38. AI 인원에 따른 UI

총 3~8명까지 화면이 깨지지 않아야 한다.

사용자:

```text
항상 화면 하단 중앙
```

AI:

```text
화면 위/좌/우에 테이블을 둘러싸도록 배치
```

### 3~4인

플레이어 영역 크게.

### 5~6인

조금 축소.

### 7~8인

원형 또는 타원형으로 배치.

각 AI에는:

```text
이름
남은 손패 수
현재 앞에 놓인 카드
연합 여부
연합 POWER
현재 턴 표시
```

를 보여준다.

AI의 손에 있는 실제 숫자는 공개하지 않는다.

---

# 39. 싱글 플레이 진행 속도

매우 중요하다.

사용자 혼자 게임할 때 AI 턴들이 너무 빠르게 넘어가면 게임 상황을 전혀 이해할 수 없다.

따라서:

**AI의 계산은 즉시 할 수 있지만 화면 연출은 반드시 순차적으로 보여준다.**

특히 AI가 여러 명이라고 AI 1~AI 7의 행동을 한 번에 처리하면 안 된다.

---

# 40. 기본 AI 턴 연출

`보통` 속도 기준:

```text
현재 AI 강조
↓
약 500ms

AI가 생각하는 연출
↓
약 500~700ms

카드 한 장 선택
↓
약 300ms

카드가 테이블로 이동
↓
약 500~700ms

카드 숫자 확인
↓
약 500ms

연합/Flush 계산
↓
이벤트가 있으면 1~1.5초 표시

다음 턴 전 대기
↓
약 500~800ms
```

단순한 AI 턴:

```text
약 2~3초
```

특별 이벤트가 있는 턴:

```text
약 3~4초
```

정도로 체감되게 한다.

---

# 41. 게임 속도 설정

사용자가 속도를 선택할 수 있도록 한다.

### 느리게

```text
AI 일반 턴 약 4초
특수 이벤트 발생 시 5초 이상
```

### 보통

```text
AI 일반 턴 약 2~3초
특수 이벤트 약 3~4초
```

기본값.

### 빠르게

```text
AI 일반 턴 약 1~1.5초
```

게임을 잘 아는 사용자용.

---

# 42. 연합이 만들어졌을 때 연출

예:

```text
나미 = 7

가희가 7 플레이
```

카드를 바로 합치지 말고 먼저 가희의 7이 화면에 등장한다.

그 후:

```text
🤝 연합!
```

표시.

그리고:

```text
7 + 7 = 14
```

를 시각적으로 보여준다.

최종적으로:

```text
연합 POWER 14
```

표시.

사용자가 **왜 14가 되었는지** 눈으로 확인할 수 있어야 한다.

---

# 43. Flush 연출

예:

```text
연수 = 6

지수 → 8
```

다음 순서로 보여준다.

```text
지수의 8 등장
↓
잠시 대기
↓
8 > 6
↓
💥 연수 FLUSH!
↓
연수의 6이 discard pile로 이동
↓
Draw Pile에서 카드 1장이 연수 쪽으로 이동
↓
다음 턴
```

8을 내는 순간 모든 애니메이션을 동시에 처리하지 않는다.

---

# 44. 연합 Flush 연출

예:

```text
7 + 7 = 14
```

상태에서 15가 나왔다.

화면:

```text
15 > 연합 14
```

표시.

그 후:

```text
💥 연합 FLUSH!
```

표시.

연합에 포함된 카드들을 같이 버리고:

각 플레이어에게 카드 1장씩 Draw 애니메이션을 보여준다.

---

# 45. 살아남은 카드 버리기 연출

현재 플레이어의 턴이 돌아왔고 activeCard가 살아 있다면:

카드를 즉시 없애지 않는다.

먼저:

```text
✨ 살아남았습니다!
```

또는 게임 UI 용어를 사용하는 경우:

```text
PUSH THROUGH!
```

표시.

카드를 강조하고 약 700~1000ms 보여준다.

그 후 discard pile로 이동한다.

그리고:

```text
남은 카드 4장
```

처럼 카드 수 감소를 사용자에게 보여준다.

---

# 46. 연합 성공 연출

나미와 가희가 7 연합이고 나미 턴까지 살아남았다면:

```text
🤝 연합 성공!
```

표시.

그 후:

```text
나미 + 가희의 7을 함께 버립니다.
```

보여준다.

두 7 카드가 동시에 discard pile로 이동한다.

그리고 두 플레이어의 남은 카드 수가 동시에 감소한다.

새 카드는 Draw하지 않는다.

그 후:

```text
나미의 차례
```

는 그대로 유지되고 나미만 새 카드를 낸다.

---

# 47. 사용자 차례는 자동 진행 금지

사용자 차례가 오면:

```text
당신의 차례입니다.
카드를 선택하세요.
```

표시.

사용자가 카드를 클릭할 때까지 기다린다.

AI처럼 자동 선택하거나 일정 시간이 지나면 자동으로 플레이하면 안 된다.

---

# 48. 게임 로그

화면에 게임 로그 영역을 만든다.

예:

```text
연수가 6을 냈습니다.

지수가 8을 냈습니다.
💥 8 > 6
연수의 6이 밀려났습니다.
연수가 카드 1장을 뽑았습니다.

나미가 7을 냈습니다.

가희가 7을 냈습니다.
🤝 연합!
7 + 7 = 14

지수의 8이 밀려났습니다.
지수가 카드 1장을 뽑았습니다.

연수가 9를 냈습니다.
7 연합의 힘 14를 이기지 못했습니다.

지수가 14를 냈습니다.
연수의 9가 밀려났습니다.

나미의 차례입니다.
🤝 나미와 가희의 7 연합이 살아남았습니다.
두 플레이어가 7을 함께 버립니다.
```

최근 약 8~12개의 주요 이벤트를 보여준다.

---

# 49. 게임 엔진과 UI 애니메이션 분리

반드시 게임 규칙과 화면 표시 코드를 분리한다.

예:

```javascript
GameEngine {
    startGame()
    playCard()
    calculateGroups()
    calculatePower()
    resolveFlush()
    resolveTurnStart()
    resolveAllianceSuccess()
    checkWinners()
    nextTurn()
}
```

그리고:

```javascript
AnimationController {
    showTurn()
    animatePlayCard()
    animateAlliance()
    animateFlush()
    animateDraw()
    animateSuccessfulDiscard()
    animateAllianceSuccess()
    animateWinner()
}
```

게임 엔진이 먼저 결과를 계산한다.

그 결과를 이벤트 큐에 넣는다.

UI는 이벤트를 순서대로 보여준다.

---

# 50. Event Queue 필수

AI 턴이 겹치는 버그를 막기 위해 하나의 이벤트 큐를 사용한다.

예:

```text
TURN_START

CHECK_EXISTING_CARD

SUCCESSFUL_DISCARD

CHECK_ALLIANCE_SUCCESS

CHECK_WINNER

PLAY_CARD

SHOW_ALLIANCE

RESOLVE_FLUSH

ANIMATE_FLUSH

ANIMATE_DRAW

WAIT

NEXT_TURN
```

각 이벤트가 끝날 때까지 다음 이벤트를 실행하지 않는다.

`async/await` 또는 Promise 기반으로 구현한다.

독립적인 여러 `setTimeout()`이 동시에 턴을 실행하는 구조는 피한다.

---

# 51. 사용자 입력 잠금

다음 상황에서는 사용자 카드 클릭을 막는다.

```text
AI 턴 진행 중
애니메이션 중
Flush 계산 중
Draw 애니메이션 중
연합 처리 중
승리 처리 중
```

사용자의 정상적인 턴에서만 카드 선택이 가능해야 한다.

한 턴에 두 장을 빠르게 클릭해서 두 카드가 나가는 버그가 발생하면 안 된다.

---

# 52. 반드시 방지해야 하는 버그

다음은 절대로 발생하면 안 된다.

```text
❌ 17, 18, 19 카드가 빠짐

❌ 3~6인인데 시작 카드가 5장

❌ 7~8인인데 시작 카드가 6장

❌ 낮은 카드를 새로 냈다는 이유로 새 카드가 바로 Flush됨

❌ 5+5의 POWER가 10이라는 이유로 실제 10 카드와 연합함

❌ 실제 숫자가 다른 카드끼리 연합함

❌ POWER가 같은 상대를 Flush함

❌ 연합 POWER 계산에 연합원이 일부 빠짐

❌ 연합이 Flush됐는데 일부 플레이어만 Draw함

❌ 연합 성공인데 현재 플레이어 카드만 버림

❌ 연합 성공 시 다른 연합원도 새 카드를 즉시 플레이함

❌ 살아남은 카드를 버린 뒤 새 카드까지 Draw함

❌ Flush됐는데 Draw하지 않음

❌ 이미 연합 성공으로 activeCard가 없어진 플레이어에게 다시 성공 처리를 함

❌ AI가 다른 플레이어의 비공개 손패를 확인함

❌ AI 턴 여러 개가 동시에 실행됨

❌ 애니메이션이 끝나기 전에 다음 턴이 시작됨

❌ 마지막 카드가 연합으로 제거됐는데 승리 처리되지 않음

❌ 공동 승리 가능한 상황에서 한 명만 승자로 처리됨
```

---

# 53. 핵심 자동 테스트

반드시 아래 테스트를 작성한다.

### TEST 1

```text
A = 6

B가 8 플레이
```

예상:

```text
A 6 Flush
A Draw 1
B 8 유지
```

---

### TEST 2

```text
A = 8

B가 7 플레이
```

예상:

```text
A 8 유지
B 7 유지
```

---

### TEST 3

```text
A = 7
B가 7 플레이
```

예상:

```text
A+B 연합
POWER = 14
```

---

### TEST 4

```text
A = 7
B = 7

C가 9 플레이
```

예상:

```text
7 연합 POWER 14 유지
C 9 유지
```

---

### TEST 5

```text
A = 7
B = 7
C = 9

D가 14 플레이
```

예상:

```text
D 14는 7 연합과 동점 → 연합 유지

14 > 9
→ C 9 Flush
→ C Draw 1
```

---

### TEST 6

```text
A = 5
B = 5

C가 실제 10 플레이
```

예상:

```text
A+B = 5 연합 POWER 10

C = 실제 10

서로 연합하지 않음

동점이므로 아무도 Flush되지 않음
```

---

### TEST 7

```text
A = 7
B = 7

연합 상태로 A의 턴 시작
```

예상:

```text
A 7 버림
B 7 버림

A Draw 없음
B Draw 없음

A만 새로운 카드 플레이
```

---

### TEST 8

TEST 7 직후 B 차례.

```text
B.activeCard === null
```

예상:

```text
B에게 기존 카드 성공 처리 없음

B가 손에서 새로운 카드 1장 플레이
```

---

### TEST 9

```text
A = 7
B = 7

C가 15 플레이
```

예상:

```text
15 > 연합 POWER 14

A Flush + Draw 1
B Flush + Draw 1
```

---

### TEST 10

AI 2명 선택.

```text
사용자 + AI2 = 총 3명
```

각 플레이어:

```text
6장
```

---

### TEST 11

AI 5명 선택.

```text
총 6명
```

각 플레이어:

```text
6장
```

---

### TEST 12

AI 6명 선택.

```text
총 7명
```

각 플레이어:

```text
5장
```

---

### TEST 13

AI 7명 선택.

```text
총 8명
```

각 플레이어:

```text
5장
```

턴 순서:

```text
Human
→ AI1
→ AI2
→ AI3
→ AI4
→ AI5
→ AI6
→ AI7
→ Human
```

---

# 54. 최종 규칙 요약

게임 엔진에서 반드시 다음 원칙을 지킨다.

### 일반 카드

```text
새 카드가 기존 카드보다 높으면
→ 기존 낮은 카드 Flush
```

### 새 카드가 더 낮음

```text
아무 일도 없음
두 카드 모두 유지
```

### 같은 숫자

```text
연합
```

### 연합 POWER

```text
실제 숫자 × 같은 숫자의 활성 카드 수
```

### 연합 조건

```text
실제 카드 숫자가 동일해야 함
```

합산된 POWER가 같다는 이유로 연합하지 않는다.

### 동점

```text
Flush 없음
```

### Flush

```text
카드 버림
+
Draw 1

전체 카드 수 감소 없음
```

### 자기 차례까지 살아남음

```text
카드 버림
+
Draw 없음

전체 카드 수 -1
```

### 연합 카드가 자기 차례까지 살아남음

```text
연합원 전체 카드 버림
+
연합원 모두 Draw 없음

각자 전체 카드 수 -1
```

현재 차례인 사람만 새로운 카드를 플레이한다.

### 승리

```text
자기 앞 카드 없음
+
손 카드 없음
```

이면 즉시 승리.

연합 때문에 다른 플레이어 턴에도 승리할 수 있고 공동 승리도 가능하다.

---

# 55. 구현 시 최우선 요구사항

이 게임은 빠르게 처리하는 것보다 **사용자가 현재 무슨 일이 일어났는지 이해할 수 있게 보여주는 것이 더 중요하다.**

특히 싱글 플레이에서 AI가 여러 명인 경우:

```text
AI 카드 등장
↓
카드 숫자 확인
↓
연합 여부 표시
↓
POWER 계산 표시
↓
Flush 대상 표시
↓
카드 제거
↓
Draw 표시
↓
결과를 잠시 보여줌
↓
다음 AI 턴
```

순서를 반드시 지켜라.

AI의 내부 계산은 즉시 끝나더라도 UI에서는 모든 행동을 순차적으로 보여준다.

기본 속도에서는 사용자가:

**"누가 무슨 카드를 냈고, 왜 저 카드가 버려졌는지"**

눈으로 따라갈 수 있어야 한다.

게임 규칙 처리의 정확성을 UI 디자인보다 우선해서 구현하고, 먼저 위 공식 예시 테스트가 정확하게 통과하도록 GameEngine을 완성한 후 UI와 애니메이션을 연결해줘.
